node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store

# --- Segredos de assinatura / release Android (NUNCA versionar) ---
android/keystore.properties
*.jks
*.keystore

# --- Firebase (config nativa de produção) ---
android/app/google-services.json

# --- Build Android ---
android/app/build/
android/build/
android/.gradle/
android/app/release/
android/app/*.aab
android/app/*.apk

# Artefatos de build/entrega (não versionar binários pesados)
*.aab
*.apk
Arquivo play store/

# CHAVE API.txt contém credencial — não versionar
CHAVE API.txt

# Firebase CLI deploy cache
.firebase/
